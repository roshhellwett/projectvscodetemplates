# Validation

This file is no longer maintained.

The active project entry points are:

- `README.md`
- `windows/install.ps1`
- `linux/install.sh`
- `linux/quiz.sh`

If you want to validate the project before publishing, test the Windows installer flow and the Linux shell scripts directly from those files.
